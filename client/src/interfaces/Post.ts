interface Post {
      title: string;
      video_url: string;
      video_ETag: string;
      img_url: string;
      img_ETag: string;
      metadata: object;
      date: Date;
}

export default Post